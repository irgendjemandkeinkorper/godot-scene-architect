# Gemini API key used by the scene generation and GDScript refinement endpoints.
# Leave it unset to use the built-in offline presets and export workflows.
GEMINI_API_KEY=""

# Optional server port. Defaults to 3000.
PORT=3000
